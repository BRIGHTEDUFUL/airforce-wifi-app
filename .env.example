# Server
PORT=3000
NODE_ENV=production
BASE_URL=http://192.168.11.10

# JWT — leave blank to auto-generate a secret on first run (saved to data/.secret)
JWT_SECRET=
JWT_EXPIRY=8h

# Optional: Gemini AI API key
GEMINI_API_KEY=
